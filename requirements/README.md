# Python requirements

The Python extraction dependencies are split by remote product so the modelling environment does not need every geospatial package.

- `patmosx.txt`: PATMOS-x cloud reconstruction.
- `era5.txt`: ERA5 weather extraction.

Core validation tests also require NumPy and pandas.
