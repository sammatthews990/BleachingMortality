# Archive

This directory preserves superseded notebooks, the chronological implementation plan, legacy scripts and figures. It provides scientific history but is outside the production pipeline.

Do not repair archived code merely to satisfy current tests. If a method is still required, extract and test it under `src/` and record the relationship in `docs/history.md`.
