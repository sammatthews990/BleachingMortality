Use `applied-ecology-writing` to draft the Methods section in `manuscript.qmd`.

Inputs & Workspace Inspection:
1. Inspect the data loading, simulation scripts, and parameter definitions in `./analysis/`.
2. Review configuration files and metadata for spatial grids, time horizons, and scenario parameters.

Drafting Specifications:
- Subsections to Create:
  * Study System & Spatial Scope (boundaries, resolution, strata).
  * Data Compilation & Preprocessing (data sources, filtering criteria, quality control).
  * Modeling Framework / Simulation Architecture (governing equations, state variables, process formulation).
  * Scenario Definitions & Sensitivity Analysis (baseline vs. intervention/counterfactual runs).
  * Statistical Validation & Uncertainty Quantification (metrics, error propagation, convergence checks).
- Code Chunks:
  * Place executable or labeled code chunks where data preparation, model runs, and parameter tables are loaded.

Style & Constraints:
- Use active voice for author workflows ("We parameterized...", "We simulated...").
- Keep mathematical notation in clean LaTeX ($...$ and $$...$$).
- Ensure explicit units for all parameters and spatial/temporal scales.