# Rendered reports

This directory contains generated HTML/PDF output and is not versioned except for this note. Executable Quarto sources live in `analysis/`.

Run `quarto render` from the repository root to rebuild the current report set.
