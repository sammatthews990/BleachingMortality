# Generated model output

Fitted objects, fold predictions, metrics, diagnostic tables and figures are written here and are not committed. Directory names should identify the generating model or assessment.

The selected fit path is declared in `config/model_registry.yml`. Existing output is not proof that the current source recreated it; use the pipeline logs and validation artefacts.
