# Executable analyses

`current/` contains the small report set that supports the registry-selected model. `investigations/` retains earlier model comparisons, environmental audits and sensitivity analyses.

Quarto renders the current set through the root `_quarto.yml`. Generated HTML belongs in `reports/` and is not versioned.
